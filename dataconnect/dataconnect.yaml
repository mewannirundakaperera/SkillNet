specVersion: "v1"
serviceId: "projectz"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "projectz-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
